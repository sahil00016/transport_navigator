name: transport_navigator
description: A Flutter app for AI-based public transport navigation.
version: 1.0.0
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  cloud_firestore: ^5.4.4
  firebase_core: ^3.6.0
  get: ^4.6.6
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true